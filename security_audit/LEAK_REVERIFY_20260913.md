# 凭据泄漏复核记录（2026-09-13 · 独立只读复核）

> 复核对象：`D:\tdsh\suno迷笛\audit\SECRET_LEAK_STATUS_CORRECTION_20260913.md` 与 AGENTS.md §8 的口径。
> **本文件不含任何凭据原值或可识别前缀**，只含计数、字段名与路径。工具在同目录（`cred_scan.py` 等），检索串一律用 bash 相邻引号拼接构造。

## 一、复核前提：修掉两个会让扫描"假绿/假阴"的方法错误

| # | 错误 | 后果 | 本次实证 |
|---|---|---|---|
| 1 | bash 里 `'xx'+'yy'` 不是拼接（求值为字面 `xx+yy`） | 检索串失效 → **恒 0 命中假绿** | 我第一轮据此报"HEAD+历史 0 命中"，改 `'xx''yy'` 后立刻命中 |
| 2 | `git log --all --regex=X` 查的是**提交信息**不是 blob | "历史维度"从未生效 | 改用 `git grep <pat> $(git rev-list --all)` 后，历史命中从 0 变 179 |
| 3 | 值字符类写窄（漏 `_ -`） | 真明文被漏成 0（**假阴**） | 那处真明文含下划线，窄字符类扫不到 |
| 4 | 字段名不做词界 | `credentials: "include"` 误判为凭据 | shell 版虚报 9 处，Python 版（带 `(?![A-Za-z0-9_])`）正确报 0 |
| 5 | 仓路径写成盘符形式（WSL 内需 `/mnt/...`） | 静默跳过 → 伪装"干净" | 首轮跨仓扫描全 SKIP |

**纪律**：任何"0 命中"必须先过**阳性对照**（拿已知含样品的输入扫出非 0）才可信。

## 二、suno-midi（公开仓）——文档结论成立 ✅

- 本地 HEAD = `75f6d7c`（文档写的 `487ed9e` 是其父提交，之后又叠了一次文档提交）；全历史 12 提交。
- **赋值型明文形态：HEAD = 0；全历史 12 提交逐个扫 = 0**（独立复验，与"filter-repo 重写后远端 0 命中"一致）。
- HEAD 那 9 处 shell 命中经查全部是 JS `credentials: "include"`，**假阳性**，已排除。
- 结论：**公开仓侧无残留，无需再动历史**。

## 三、第二大脑（私有仓）——HEAD 结论成立 ✅，但**历史口径严重低估** ❌

HEAD 明文赋值 **3 处**（与文档精确一致，逐一独立证实；三处值长度=21、指纹=大写+符号，判定为**同一枚旧码**）：

| # | 路径 | 性质 |
|---|---|---|
| 1 | `automation/stock/config.py` | **活动代码里的真明文**（唯一需立即改的功能性泄漏；重置授权码后须同步此文件，否则 stock 邮件断） |
| 2 | `99_archive/obsidian-vault-old_J盘/automation/stock/config.py` | 归档旧副本 |
| 3 | `OH-Works/Tomoko-activity/script-audit-report-20260811.md` | **记录型泄漏**：审计报告把字段连值抄进文档（正是 §8 那条纪律的现实案例） |

含该字段名的跟踪文件 = **13 个**（文档写 12；差的 1 个是 2026-09-13 新建的交接摘要，属正常漂移）。其余为 environ 引用/仅提字段名，明文 0。

### ⚠️ 需更正：历史暴露面不是"4 提交"

| 口径 | 数字 | 意义 |
|---|---|---|
| `git log -S<字段名>`（出现次数变化的提交） | **1**（引入提交 `60b2d15e`） | 文档"4 提交"属此类计数口径 |
| **树中含真值形态赋值的提交** | **161 / 215**（初值 179/185 已作废：未剔除 `REDACTED-…` 占位符，且并发会话使总数 185→215） | **这才是暴露面**：clone 后 checkout 几乎任一历史提交都能读到明文；HEAD 侧仅占位符（见 §八） |

**影响**：若按"只有 4 个提交"判断，会低估清洗必要性、可能误判"改 HEAD 就够了"。实际只要仓被 clone/授权可见，**整条历史几乎都含该码**（私有仓，风险限于授权账号与任何 fork/下游 clone）。
→ 处置仍以**重置授权码**为唯一彻底解（一次动作令 HEAD 3 处 + 179 个历史树里的旧串全部失效）；历史 filter-repo 属可选洁癖项，且本仓有**其他会话并发提交**（复核期间提交数从 182→185 漂移），执行前必须先与其他会话协调，避免 force-push 撞掉别人的提交。

## 四、待用户（未擅动任何东西）

1. 【必做·根治】重置 QQ SMTP 授权码（tuanshen@qq.com）→ 令上述所有旧串失效。
2. 【必做·跟改动】重置后同步 `automation/stock/config.py` 改环境变量注入，否则 stock 邮件断。
3. 【建议】第 3 处（OH-Works 审计报告）按 §8 改为"只写字段名+状态"。
4. 【可选】私有仓历史 filter-repo 抹串——**须先协调并发会话**，并按 §7 做"HEAD 变化 + 远端 fresh clone 复扫 0 命中"双验证。
5. 【建议】AGENTS.md §8 补两条：①值字符类须含 `_ -`；②"历史维度"必须用 `git grep <pat> $(git rev-list --all)`，并注明 `-S` 口径与"树中含明文"口径的差别（本次低估即源于混用）。

## 五、边界说明

本次全程只读：未修改任何仓的工作树/历史/remote，未 kill/启动进程，未把凭据详单写入**公开仓**（详单只落本私有目录）。

## 六、备份状态提示（重要）

- 本轮已把上述方法教训并入 `AGENTS.md §8`（现共 **8 条**：本文件 §一 的 5 条 + 下列 3 条）。
- **但 `AGENTS.md` 位于被 gitignore 的 `D:\tdsh\dsh-home\` 内 → 该全局指令修正不在任何 GitHub 备份中**，机器重装/目录丢失即失效。本文件（TDSH 私有仓内）即其备份副本。
- 未记在 §一 表里的另外 3 条：
  6. `git grep <pat> $(git rev-list --all)` 在大仓会爆参数/超时（实测 240s 无返回）→ 必须 **path-scoped** 取提交子集；且并发会话会让提交总数漂移（182→185），**报数须注明时点**。
  7. **两工具结论冲突必须查实，不许挑一个信**：本轮 shell 报 9 / Python 报 0，实为两处**方向相反**的缺陷并存（shell 既假阳又假阴），两者都不对、需第三版。数字不一致是信号不是噪声。
  8. 假阳性会**制造"我扫到东西了"的错觉**，反过来掩盖真假阴 → 扫描器须同时给出逐条判读依据（字段名 / 值长度 / 字符类别指纹），不能只报一个总数。
- 待用户决定：是否给 `dsh-home` 建**私有**备份（须先剥离其中 settings 里的 token，不宜裸推）。


---

## 七、会话独立印证（2026-09-13 10:5x 追加，另一 DSH 会话只读复核）

> 本段由另一 DSH 会话独立执行，与 §一~六 结论交叉验证。全程只读：未改工作树/历史/remote，未输出任何凭据值（只列路径/计数/值指纹）。

### 7.1 方法（符合 §8 纪律）
- 阳性对照：先在临时文件验证检索正则能命中（dummy 样本），删除后正式扫（§8-3）。
- path-scoped：大仓 `git grep HEAD` 全扫超时（240s 无返回，§8-6 复现）→ 改用 `git ls-tree -rz HEAD` 取目标路径子集 + `git show HEAD:<path>` 定向检，规避中文路径转义（`\347\233\230`="盘"）。
- 只输出：路径 / 赋值型明文命中数 / 是否含字段名；正则 `SENDER_AUTH\s*=\s*['"][A-Za-z0-9_-]{8,32}['"]`（值字符类含 `_ -`，§8-4）。

### 7.2 复核结果（HEAD=1d280d9，时点提交总数 50）

| # | 路径 | 赋值型明文命中 | 含字段名 |
|---|---|---|---|
| 1 | `automation/stock/config.py`（活动代码） | 1 | True |
| 2 | `99_archive/obsidian-vault-old_J盘/automation/stock/config.py`（归档旧副本） | 1 | True |
| 3 | `OH-Works/Tomoko-activity/script-audit-report-20260811.md`（记录型泄漏） | 1 | True |

**结论**：与 §三（HEAD 树 3 处明文，同一枚旧码=长度 21、大写+符号指纹）**完全一致**，独立印证成立。处置待用户：重置授权码（根治）+ 同步改 `automation/stock/config.py` 为环境变量注入。本会话未擅动任何东西。

> ⚠️ **本段结论已被 §八 裁定修正**：其正则 `SENDER_AUTH\s*=\s*['"][A-Za-z0-9_-]{8,32}['"]` **不区分占位符与真值**（§8-9 失效模式），因此"HEAD 3 处=同一枚旧码（活凭据）"的说法只对历史，不对 HEAD。

## 八、跨会话分歧裁定（11:0x，`resolve_placeholder_vs_real.py`）

§七 与本文件 §三更正互相矛盾：一方称 HEAD 三处是"同一枚旧码=赋值型明文"，另一方（我）称它们是 `REDACTED-…` 占位符。按 §8-8「冲突必须查实」，用**值的长度+字符类别+占位词前缀**（绝不输出值）逐提交裁定：

| 位置 | 旧提交值形态 | 新提交（`865ff96` 脱敏提交）值形态 |
|---|---|---|
| I: 活动 `automation/stock/config.py` | **16 位纯小写**（`60b2d15`）●真凭据形态 | **21 位大写+下划线** ★占位符形态 |
| I: 归档 `99_archive/…/config.py` | 16 位纯小写（`70fc775`）● | 21 位 ★ |
| I: `OH-Works/…/script-audit-report-20260811.md` | 16 位纯小写（`60b2d15`）● | 21 位 ★ |
| H: 同上三处 | 16 位纯小写（`c9dfd02`）● | 21 位 ★ |

**裁定：两方各对一半，分歧来自"看的维度不同"**
- **HEAD 不含活凭据**（是占位符）→ §三更正成立，§七"HEAD 有明文真码"不成立。
- **历史确实含真码**（16 位纯小写 = QQ SMTP 授权码典型形态，脱敏提交之前人人可得）→ §七担心的"同一枚旧码"在历史维度成立。
- 我此前报的"含真值形态提交 = 161/215"与此完全自洽（真值存在于 `865ff96` 之前的所有提交树）。
- 另发现：**存在两个第二大脑仓**（`I:\Obsidian\vaults\第二大脑` 222 提交 / `H:\second-brain` 74 提交，HEAD 同为 `f101a90e`），两处都有同样的历史真码 → 清洗范围要按 §8 跨仓纪律**两个仓都算**，不能只处理一个。

**待用户（不变，且范围扩大）**：U1 重置授权码（一次令两个仓的全部历史真码失效，仍是唯一彻底解）｜U2 活动 config 改 env 注入（当前邮件功能坏）｜U4 两仓历史清洗（并发会话在提交，须先协调）。

## 九、跨仓补扫与"我自己造的两个新坑"（11:3x）

### 11.1 补上先前清点的盲区
上一轮"24 仓普查"的仓盘点只走了 4 个根 + 深度限制，**漏了 H 盘另外 4 个仓**（`sb-verify`、`hs-research/{AutoHearthStone,HsMod,Sabberstone}`）。补扫结果（按字段名→再对命中文件做形状判定，绝不输出值）：

| 仓 | 含字段名的 HEAD 文件 | 真值形态 | 占位符 | 判定 |
|---|---|---|---|---|
| `H:\sb-verify` | 12 | **0** | 2 | HEAD 无活凭据 |
| `H:\second-brain` | 0（活动 config 已不在工作树） | 0 | 0 | 该项 HEAD 干净；历史仍含真码（`c9dfd02`） |
| `H:\hs-research\*` 三仓 | 0 | 0 | 0 | 无关 |
| `D:\tdsh` | 5 | 见 11.2 | — | 全部是我自己审计工具的合成样本 |
| `J:\minicpm5-clone` | 1 | 0 | — | 同上（`detector_controls.py` 里的 FAKE 值） |

→ **U1/U4 的范围结论不变**：真码只存在于两个第二大脑仓的**历史**，HEAD 均无活凭据。

### 11.2 我自己的两处工具缺陷（这次被自己抓到）
1. **形状启发式过松**：`credentials: "include"`（JS 选项）被判成"●真值形态"（≤20 位纯小写）。→ 真值形态判定必须加**词典词/非密钥语义词排除**与最小长度（≥12），否则审计文档里的 JS 片段会被当成泄漏。
2. **审计工具自身成为扫描噪音**：为做阳性对照而写入的合成值（`REDACTED-QQ-SMTP-XXXX`、`Abc123_xyz…`、`apiKeyEnv = "DEEPSEEK_API_KEY"` 等）会**被后续凭据扫描命中**。本次 5 处"命中"全是这个。→ 建议：合成对照值统一加显式假值前缀（本目录已用 `FAKE`/`REDACTED`/`example.invalid`/TEST-NET 段），并在扫描器里对这些前缀/保留域做 allowlist；写进文档时注明"这是对照样本不是凭据"。

### 11.3 顺带证伪的一次假绿（§8-7 复发）
`shape_all_repos.py` 首版在 WSL 里用 `H:/sb-verify` 形式的盘符路径 → 九仓全部"路径不存在→跳过"，聚合结果**报 0 命中**，看起来完全干净。因脚本本身打印了 `SKIP（跳过≠干净）` 才没被我采信。**该定则再次证明自己有用**。



## 十、两条全局定则追加（由"对照台自己连错两次"得出，已并入 AGENTS.md §8 第 10/11 条）

10. **判据禁止用"子串包含"**：`"WARN" not in out` 因输出含 `WARN=0` 而永假；`"PASS" in ob` 拿错变量。必须解析数值。
11. **对照件本身要被对照**：先喂"已知应亮/已知应灭"的合成样本，再拿门判真数据。
→ 工程化：`J:\minicpm5-clone\scripts\detector_controls.py`（7 类 PII 正则、脱敏门 4 判定、打分器双向对照，全用假造值）。本轮实测三组检测器**全部点亮** → 此前"身份证=0""NOT_FIXED""v0.2 内容轴 2 FAIL"三个结论可采信，非假绿。

